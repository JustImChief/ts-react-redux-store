import store from './Store';
export default store;
export { connectReducers, dispatch, getState, Reducer, registerReducer, types, unregisterReducer, } from './Store';
export type { InitialStateType, StateType } from './Store';
